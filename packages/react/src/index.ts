// Import styles at the top level to ensure they're bundled
import './styles/index.css';

// Barrel export for React components
export * from './components';
export * from './providers/ThemeProvider';
